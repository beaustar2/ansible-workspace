---
- name: Install OS
  hosts: g4
  become: yes

  tasks:
    - name: Check OS Distribution
      command: "cat /etc/os-release"
      register: os_release_output

    - name: Install CentOS
      yum:
        name: "{{ 'centos-release' }}"
      when: "'CentOS Linux 7' in os_release_output.stdout"

    - name: Install Debian
      apt:
        name: "{{ 'Debian GNU/Linux' }}"
      when: "'Debian GNU/Linux 12' in os_release_output.stdout"
...